import { isEven } from "./condicionales";
import { add } from "./buclesFor";


let resultado1:void = isEven(add(["Casa", "Coche", "Ciudad", "Cesta"]))
let resultado2:void = isEven(add(["Barco", "Baca", "Bicicleta", "Balon", "Bisiesto", "Brasil"]))
let resultado3:void = isEven(add(["Venezuela", "Veneno", "Voltaje"]))

// gracias a la funcion isEven todo se devuelve por consola

